{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "npm run watch & nuxt dev --no-clear",
    "watch": "nodemon modules/doczilla/sync.js --watch ../docs -e md,vue --watch modules/doczilla/sync.js",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "overrides": {
    "vue": "latest"
  },
  "dependencies": {
    "lodash.clonedeep": "^4.5.0",
    "pinia": "^2.0.35",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@nuxt/content": "^2.6.0",
    "@pinia/nuxt": "^0.4.9",
    "fs-extra": "^11.1.1",
    "nodemon": "^2.0.22",
    "nuxt": "^3.4.0",
    "nuxt-simple-sitemap": "^2.6.2",
    "sass": "^1.62.0"
  }
}
